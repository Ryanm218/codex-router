# Codex Router desktop packaging repair, 2026-09-05

Goal: finish Ryan's requested update of the already reviewed/pushed router 2e0a655de50e7c05d84d7b556d661db4f9ed76a7. Backend 0.5.1 is healthy. Preserve providers, accounts, policies, authentication, and Codex process. No inference requests.

Observed: stable checkout clean; exact remote parity. Desktop build and 96 passing browser/Control Center tests (one Windows skip) succeeded. Old Model Router.app is incomplete/signature invalid; source-root and launch entry attest ownership. Preserved supervisor backup at ~/Applications/.codex-router-backup-20260905-2e0a655. Attempted replacement through bin/model-router-tray stops in draining because its own --quit-for-update helper never exits. Only our exact installer/helper were terminated and its handler restored the old app path. The package has src/chatgpt-login-lease.mjs, but ipc.mjs top-level packaged URLs for src/chatgpt-account-operation-lock.mjs and src/chatgpt-request-use-lease.mjs do not exist. Those files are present only under router-src. Their dependency proper-lockfile and its three transitive dependencies are absent from external resources too.

Proposed smallest coherent repair: replace the five individual source mappings in apps/control-center/electron-builder.yml with the complete existing ../../src tree at Resources/src, and copy the already locked proper-lockfile, graceful-fs, retry, signal-exit package directories to Resources/node_modules. This lets both existing Resources/src and Resources/router-src resolve the same locked dependencies. Do not change runtime imports, lease/security/account behavior, provider policy, or dependency versions/locks. This duplicates source resources by a few MB but avoids an incomplete hand-maintained import closure and preserves the existing signed bundled-module authority boundary. Do not load modules from a mutable external checkout as an alternative fix.

Verification: add a focused executable regression that stages the resource mappings actually declared by electron-builder.yml in a private temporary Resources tree and imports both account modules in a fresh Node process. Require expected exports; a removed proper-lockfile dependency must fail. RED on base mapping, GREEN on repaired mapping. Inspect all mapped paths, verify package graph matches lockfile, run existing Control Center/renderer tests, build actual signed package, confirm --query-lifecycle returns bounded JSON promptly and --quit-for-update exits promptly, then the supported installer must reach committed with exact new host and renderer readiness. Run doctor, strict codesign, source-root assertion, matching tray fingerprint, clean git status, push parity. Keep prior app/supervisor rollback copies. Final independent Fable review before commit/push; complete attended local reinstall from stable reviewed source.

Scope is a packaging repair required by installation, not an account-fallback redesign. The original Fable design/final evidence is in docs/reviews/2026-09-05-openai-account-quota-fallback-fable-design-cycle-4.md and the last OpenAI-account review in ~/.claude/logs/fable-final-review.log.

## Approved design

Fable design approved on 2026-09-05, successful command exit 0.

FABLE_DESIGN: PASS

Implementation corrections: replace all five individual src mappings with the full directory mapping (no overlapping concurrent copies); every resource source must exist; test all 13 eager modules plus chatgpt-profile-switch in a fresh process; gate the desktop build by adding the regression to its npm test list. The mirror intentionally does not support arbitrary router command entrypoints that read package metadata from the resource root.

Approval evidence: /private/tmp/codex-router-desktop-repair/design-result.txt and the corresponding 2026-09-05 entry in the private fable-design-review.log.

## Installed verification before final review

- The full import regression was RED on the original mappings (missing account-operation-lock module), then GREEN with all 13 eager modules plus the profile-switch dynamic hop. Deleting the staged proper-lockfile makes the fresh-process negative control fail. Every declared resource source must exist; every mapping must parse; the test is part of the Control Center build gate.
- Root syntax checks pass. The signed release build passes 97 Control Center/renderer tests with one expected Windows-only skip. No runtime source or lockfile/dependency version changed; the root-wide account/protocol suite is outside this packaging delta.
- The supported tray installer completed with exit 0, verified the exact native host and hidden renderer, stopped the hidden probe cleanly, and removed its transaction directory. Strict deep codesign verification passes. Installed app version is 0.5.1 and signed ModelRouterSourceRoot is /Users/ryan/.local/share/codex-router.
- Both packaged helpers now exit 0 promptly: query-lifecycle in 0.14 seconds, quit-for-update in 0.12 seconds. The query reports no lingering hidden child, as expected after the installer probe. Tray service is installed, loaded, app present, running; tray planning reports skip. The supported doctor returns ok with no failed checks. Astra and Kimi K3 remain in the merged catalog.
- Local evidence: /private/tmp/codex-router-desktop-repair/{install.log,verification.json,doctor.json,full-regression-red.txt}. No inference, account activation, credential inspection, or Codex restart was performed.
- The original incomplete app and supervisor are preserved at /Users/ryan/Applications/.codex-router-backup-20260905-2e0a655. The original app fails signature validation and is recovery evidence, not a launchable verified rollback. For source rollback, revert this packaging commit and rebuild from a separately verified compatible revision; do not restore credentials or delete retained backups.
- Independent final Fable review completed successfully on 2026-09-05 with exactly one standalone FABLE_REVIEW: PASS. It independently verified the complete source and dependency byte parity, link counts, strict signatures, five-file scope, and build-gating tests. Approval evidence: /private/tmp/codex-router-desktop-repair/final-result.txt and the corresponding private fable-final-review.log entry. This commit records that approval; the live install manifest records the final published source revision.
