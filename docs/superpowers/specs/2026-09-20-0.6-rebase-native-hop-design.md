# 0.6 rebase + native hop + #787 — design record

Date: 2026-09-20
Goal: rebase live unique commits onto origin/main (0.6.0 + later), then cherry-pick #787 auto-review quota fallback, in an isolated worktree. Live checkout stays on fa71aefc until tests pass.

## Independent review

Astra Medium: UNAVAILABLE (Codex usage cap; retry 2026-09-21 14:54). Command recorded ASTRA_EXIT:1 with usage-limit error.
Fable 5.1: UNAVAILABLE (Claude org disabled Claude Code subscription; no Anthropic API key). FABLE_EXIT:1.
User authorization in the active task: "Please go ahead now as I am done for 3.5h" after the 0.6.0 rebase + #787 plan was stated. Earlier same task: "Please go ahead. We have no Astra usage limit left" / "No other usage available".

## Architecture

Worktree: /Users/ryan/Code/Codex/codex-router-0.6-rebase on feat/0.6-rebase-native-hop at origin/main 80f11c07.

Cherry-pick unique live commits oldest-first:
2e0a655d account quota fallback
246cf404 desktop package account modules
3b5031ae grok id-first tool schemas
35c3f903 grok 4xx/429/prelude
02fb0f18 grok oauth session + socket
b0a4fe92 grok chat.completions error shape
d87d6073 grok automation_update mode enums
caf2a970 assistant notes fold
fa71aefc native ChatGPT hop from routed quota miss

Then cherry-pick a43b6af8 (#787 auto-review fallback). Scope remains codex-auto-review only.

origin/main already has ChatGPT account switching (#563) but not the quota-hop pool (708 vs 1158 lines). Native hop control (`failover native`) is not on origin/main.

Keep: Grok primary, native hop default-on only via existing live failover.json after install, two-account pool default-off.

## Failure modes

- Cherry-pick conflicts in router.mjs / control.mjs / grok-oauth-forwarder.mjs / model-failover.mjs
- Double-applying account-pool onto #563 switching
- 787 colliding with native hop in router.mjs
- Tests fail; live must remain fa71aefc
- Disk pressure (~16Gi free)

## Verification

npm test focused: account pool/fallback, model-failover, grok-oauth, auto-review-fallback, routing.
Do not install over live until those pass.
